Once the user creates account and logs in the website. The user will be asked to accept cookies or reject. If the user accepts the cookies. 
The username will get saved as cookie. Whenever user enters the website next time he does not have to login. Once the user logout all the cookies will get destroyed and the
user has to login again.

After logging in the use will be redirected to a webpage where an api is called. The api delevers a list of memes among wich random five memes are displayed to user.
